apiVersion: v1
kind: Service
metadata:
  name: ebad-backend
  labels:
    app: ebad-backend
spec:
  selector:
    app: ebad-backend
  ports:
    - name: "13000"
      port: 13000
      targetPort: 13000
